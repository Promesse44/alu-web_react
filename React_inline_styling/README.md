# React Inline Styling

This project contains the dashboard app used for the inline styling task.

It includes the app structure and exercises for styling table rows using inline styles and React component props.

## Project structure

- task_0/dashboard

## Run the app

cd task_0/dashboard
npm install
npm start
